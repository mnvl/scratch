
COLLECT_SOURCE_FILES(SPATIAL_SOURCES SPATIAL_TEST_SOURCES)

ADD_LIBRARY(spatial STATIC ${SPATIAL_SOURCES})

ADD_EXECUTABLE(test_spatial ${SPATIAL_TEST_SOURCES})
TARGET_LINK_LIBRARIES(test_spatial spatial math util ${SCRATCH_LIBRARIES})
ADD_CONFIGURATION_DEPENDENT_TEST(spatial test_spatial)
