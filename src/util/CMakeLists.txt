
COLLECT_SOURCE_FILES(UTIL_SOURCES UTIL_TEST_SOURCES)

ADD_LIBRARY(util STATIC ${UTIL_SOURCES})

ADD_EXECUTABLE(test_util ${UTIL_TEST_SOURCES})
TARGET_LINK_LIBRARIES(test_util util ${SCRATCH_LIBRARIES})

ADD_CONFIGURATION_DEPENDENT_TEST(util test_util)
