
COLLECT_SOURCE_FILES(MATH_SOURCES MATH_TEST_SOURCES)

ADD_LIBRARY(math STATIC ${MATH_SOURCES})

ADD_EXECUTABLE(test_math ${MATH_TEST_SOURCES})
TARGET_LINK_LIBRARIES(test_math math util ${SCRATCH_LIBRARIES})

ADD_CONFIGURATION_DEPENDENT_TEST(math test_math)
