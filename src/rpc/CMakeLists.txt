
COLLECT_SOURCE_FILES(RPC_SOURCES RPC_TEST_SOURCES)

ADD_LIBRARY(rpc STATIC ${RPC_SOURCES})

ADD_EXECUTABLE(test_rpc ${RPC_TEST_SOURCES})
TARGET_LINK_LIBRARIES(test_rpc rpc util ${SCRATCH_LIBRARIES})

ADD_CONFIGURATION_DEPENDENT_TEST(rpc test_rpc)
